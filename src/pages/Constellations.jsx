import ConstellationLayout from "../components/ConstellationLayout";

export default function Galaxies() {


    // console.log(constellations.length);
    // constellations.forEach(item => {
    //     if(getSpaceObjectInfo(item.label).length) {
    //         numberOfData++
    //         console.log(item.label + " - " + getSpaceObjectInfo(item.label).length)
    //     }
    //     console.log({numberOfData})
    // })


    return (
        <>
            <ConstellationLayout />
        </>
    )
}